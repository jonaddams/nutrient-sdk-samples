// The viewer behavior is identical to the build-time sibling — re-export
// to avoid duplication. If we ever need server-specific viewer behavior we
// can fork at that point.
export { SearchViewer } from "@/app/web-sdk/indexed-search/SearchViewer";
